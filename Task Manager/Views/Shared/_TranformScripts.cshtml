<script>
    const bottons = document.querySelectorAll(".list-group-item-action");
    const showDetails = document.querySelectorAll(".show-detail");

    bottons.forEach((button, i) => {
        button.addEventListener("click", (event) => {
            event.stopPropagation();
            bottons.forEach((b) => {
                b.classList.remove("active");
            });
            button.classList.add("active");

            if(showDetails[i].classList.value == "row show-detail")
                showDetails[i].classList.add("active");
            else
                showDetails[i].classList.remove("active");
        });
    });

    document.addEventListener("click", () => {
        const activeBottons = document.querySelectorAll(".list-group-item-action.active");
        if (activeBottons.length > 0) {
            activeBottons.forEach((activeBotton) => {
                activeBotton.classList.remove("active");
            });
        }
    });

    //showDetails.forEach((showDetail) => {
    //    showDetail.addEventListener("click", () => {
    //        showDetails.forEach((sd) => {
    //            sd.classList.remove("active");
    //        });
    //    showDetail.classList.add("active");
    //    console.log("hello");
    //    })
    //});

</script>